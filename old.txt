
                    # select_elements = new_page.locator('//select')

                    # for i in range(select_elements.count()):
                    #     select = select_elements.nth(i)

                    #     try:
                    #         # Check if the select has an empty selected value with a 2s timeout
                    #         selected_option = select.locator('option[selected]').get_attribute("value", timeout=2000)

                    #         if not selected_option:  # If no value is selected
                    #             print(f"Select #{i + 1} has no value selected.")

                    #             # Get all option texts
                    #             options = select.locator('option')
                    #             option_texts = [option.inner_text() for option in options.all()]
                                
                    #             print(f"Available options: {option_texts}")

                    #     except TimeoutError:
                    #         print(f"Select #{i + 1} timed out while checking selected value.")




                    # grandparent_div = new_page.locator("//span[text() = 'Please make a selection']/ancestor::div[4]")
                    # grandparent_div.wait_for(state="visible", timeout=1000) 

                    # # Extract all text inside the div
                    # div_text = grandparent_div.inner_text()

                    # print("Extracted Text:")
                    # print(div_text)